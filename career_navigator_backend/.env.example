# Backend environment example for Career Navigator Backend

# Comma-separated list of allowed CORS origins. Do NOT over-permit.
# Example includes local dev and preview frontend URL.
ALLOWED_CORS_ORIGINS=http://localhost:3000,https://vscode-internal-11652-beta.beta01.cloud.kavia.ai:3000

# Optional: SQLite database URL (defaults to ./data/app.db if not set)
# DATABASE_URL=sqlite:///./data/app.db
