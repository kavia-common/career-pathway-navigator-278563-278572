# Backend configuration for Career Navigator
# Note: The orchestrator sets env vars; do not commit real secrets.

# Optional: override default SQLite path (defaults to ./data/app.db)
# Use sqlite:///absolute/or/relative/path.db
# DATABASE_URL=sqlite:///./data/app.db
